import axios from 'axios';

export default async function saveFavoriteCat({ id, url, user_id }) {
  // save favorite to db ... make random users until we have auth

  // then return list of all favorites

  return { favorites: [{ id: '1' }] };
}
